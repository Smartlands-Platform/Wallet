import { connect } from 'react-redux';import TabsPanel from './component';import { getOrderbook} from '../../../selectors/stellarData';import { toggleNavigation } from '../../../actions/ui';import { toggleNav } from '../../../selectors/ui';import {    getKeypair,} from '../../../selectors/account';const mapDispatchToProps = dispatch => ({  closeNavigation: () => dispatch(toggleNavigation(false))});const mapStateToProps = state => ({  toggle: toggleNav(state),    orderbook: getOrderbook(state),    keypair: getKeypair(state),});export default connect(mapStateToProps, mapDispatchToProps)(TabsPanel);